# Copy this file to .env and fill in real values. Never commit the real .env.

ENV=development

DATABASE_URL=postgresql+psycopg://ids_user:ids_pass@localhost:5432/ids_db

SECRET_KEY=replace-with-a-long-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=60

ACTIVE_MODEL_PATH=app/ml/artifacts/best_model.pkl
MODEL_FEATURE_LIST_PATH=app/ml/artifacts/feature_columns.json
